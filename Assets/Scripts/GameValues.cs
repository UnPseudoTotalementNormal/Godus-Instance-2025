using UnityEngine;

    public static class GameValues
    {
        public const int MAX_TILE_HEIGHT = 10;
        
        public static readonly Vector2 GRID_OFFSET = new Vector2(0.5f, 0.5f);
    }